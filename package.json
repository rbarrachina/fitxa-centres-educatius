{
  "name": "fitxa-centres-educatius",
  "version": "1.0.0",
  "description": "Aplicacio inicial per carregar la fitxa de centres educatius des del portal de la Generalitat a partir del codi de centre.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:github-pages": "npm run build && node scripts/build-github-pages.mjs",
    "build:watch": "tsc -p tsconfig.json --watch",
    "build:serveis-educatius": "python3 scripts/build_serveis_educatius.py",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000 --directory web",
    "serve:seo": "node scripts/serve-local.mjs",
    "backend": "python3 -m backend.server"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-or-later",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
